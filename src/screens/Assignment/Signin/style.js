import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 24,
    },

    button: {
        marginVertical: 20,
    },

    footerText: {
        color: 'blue',
        marginBottom: 56,
        textAlign: 'center',
    },

    footerLink: {
        fontWeight: 'bold',
    },
});